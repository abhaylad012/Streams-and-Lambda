package com.Question2;

import java.util.List;
import java.util.function.Predicate;
import java.util.stream.Collectors;

public class PredicatePriceStatus {
	
	
	/*public static Predicate<Orders> filterByPrice(){
		return result -> (result.getPrice()> 10000);
	}
	
	public static Predicate<Orders> filterByStatus(){
		return result1 -> (result1.getStatus()=="Accepted" || result1.getStatus()=="Accepted");
		
	}

	public static Object filterOrders(List<Orders> order1, Predicate<Orders> predicate) {
		
		return order1.stream().filter(predicate).collect(Collectors.toList());
	}*/

}

